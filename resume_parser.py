# This file is temporarily disabled to prevent a recurring crash.
# The underlying bug is in the llm_client.py file and will be fixed separately.

if __name__ == "__main__":
    print("Resume parser is temporarily disabled. Please run a different workflow.")
